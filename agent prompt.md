AI Agent Prompt: Calcy (Vanilla Stack Edition)Role: You are a Senior Frontend Engineer specializing in high-performance, SEO-optimized utility tools.Project: Build "Calcy"—a modern, one-page multi-calculator dashboard using Vanilla HTML, Tailwind CSS, and ES6+ JavaScript.Phase 1: The Core Structure (Tailwind + HTML5)Layout: Create a single index.html with a clean 2026 "Bento Box" dashboard.The Shell: Use a 3-column desktop layout:Left: A scrollable "Category" menu.Center: The "Active Tool" stage.Right: A dedicated AdSense Sidebar (Fixed width: 300px).Navigation: Implement a central search bar at the top. Use a simple JavaScript array to filter tools.AdSense Slots: Insert 3 div placeholders with clear borders and "Advertisement" labels.Constraint: Ensure exactly 24px of white space between ad slots and buttons to prevent accidental click penalties.Phase 2: Modular Calculator Engine (JS Logic)Task: Build calculators as independent JS modules or functions that inject HTML into the "Active Tool" stage.Verification Rule: Build one tool at a time. STOP and verify the math against standard benchmarks before moving on.Calculator Set 1:Mortgage: Monthly payment, total interest, amortization schedule (table).BMI/TDEE: Imperial/Metric toggle, activity level multipliers.Unit Converter: Length, Mass, Temperature, and Data Storage.State Management: Use standard JS Event Listeners (input) for real-time calculation. No "Calculate" button needed—results should update as the user types.Phase 3: SEO-First ContentFor each tool, you must create an article section below the calculator.Required Content: A header, a 3-sentence explanation of the tool, and the mathematical formula written in standard text (e.g., $BMI = kg / m^2$).Why: This provides the "original text content" AdSense bots require for site approval.Phase 4: Performance & DeploymentOptimization: Use Tailwind via Play CDN for development, but output a minified CSS file for production.Persistence: Use localStorage to save the user's 3 most recently used calculators at the top of the dashboard.Zero Dependencies: Do not use any external JS libraries (like jQuery or Lodash). Use pure ES6.